model_config_list {
  config {
    name: 'initial_inference'
    base_path: '/models/initial_inference'
    model_platform: "tensorflow"
  }
  config {
    name: 'recurrent_inference'
    base_path: '/models/recurrent_inference'
    model_platform: "tensorflow"
  }
}
